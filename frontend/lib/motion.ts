// Optimized framer-motion imports - only what we use
export { 
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useMotionValue,
  useTransform
} from 'framer-motion'

// Types
export type { 
  MotionProps,
  AnimationControls,
  Variants,
  Transition 
} from 'framer-motion'